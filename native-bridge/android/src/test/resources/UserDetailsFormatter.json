{
  "androidNotification": "notification = ${userAlias},${email},${firstName},${imageUrl},${displayName},${lastName},${nickName}",
  "default": "${userAlias},${email},${firstName},${imageUrl},${displayName},${lastName},${nickName}"
}